listen_addresses = '*'  # Allow connections from all IPs
port = 5432             # Default PostgreSQL port
max_connections = 100   # Adjust based on your needs
shared_buffers = 128MB  # Adjust based on your server resources